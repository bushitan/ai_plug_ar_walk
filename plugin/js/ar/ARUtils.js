
var ARBase = require("ARBase.js")

/**
 * @method  AR的工具类
 */
class ARUtils extends ARBase  {
    constructor(options) {
        super(options)
        // if (!options.GP) { throw Error('GP值不能为空'); }
    }
}